apiVersion: v1
kind: Service
metadata:
  name: saveinvest-service
spec:
  type: NodePort
  ports:
    - port: 80  # Port on the service
      targetPort: 3078  # Port on the pods
      nodePort: 30078  # Optional: If not specified, Kubernetes will allocate a random port from the NodePort range
  selector:
    app: saveinvest-service  # Label selector to match the pods you want to expose


# apiVersion: v1
# kind: Service
# metadata:
#   annotations:
#     kompose.cmd: kompose convert -f docker-compose.yml
#     kompose.version: 1.26.1 (HEAD)
#   creationTimestamp: null
#   labels:
#     io.kompose.service: saveinvest-service
#   name: saveinvest-service
# spec:
#   ports:
#     - name: "3078"
#       port: 80
#       targetPort: 3078
#   selector:
#     io.kompose.service: saveinvest-service
# status:
#   loadBalancer: {}
