apiVersion: v1
kind: Service
metadata:
  name: symplus-service
spec:
  type: NodePort
  ports:
    - port: 80  # Port on the service
      targetPort: 3011  # Port on the pods
      nodePort: 30011  # Optional: If not specified, Kubernetes will allocate a random port from the NodePort range
  selector:
    app: symplus-service  # Label selector to match the pods you want to expose



# apiVersion: v1
# kind: Service
# metadata:
#   annotations:
#     kompose.cmd: kompose convert -f docker-compose.yml
#     kompose.version: 1.26.1 (HEAD)
#   creationTimestamp: null
#   labels:
#     io.kompose.service: symplus-service
#   name: symplus-service
# spec:
#   ports:
#     - name: "3048"
#       port: 80
#       targetPort: 3048
#   selector:
#     io.kompose.service: symplus-service
# status:
#   loadBalancer: {}
